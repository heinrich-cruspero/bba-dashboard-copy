<%= form_with(model: want_list, local: true) do |form| %>
  <% if want_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(want_list.errors.count, "error") %> prohibited this want_list from being saved:</h2>

      <ul>
      <% want_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :want_list_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :want_list_user_id %>
  </div>

  <div class="field">
    <%= form.label :want_list_privacy_id %>
    <%= form.text_field :want_list_privacy_id, id: :want_list_want_list_privacy_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
